<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>현웅 게시판</title>
  </head>
  <body>
    <h1><a href='/main'>현웅 첫번째 게시판</a></h1>
    <hr>
    <ol>
      <% for(var i = 0; i< results.length; i++){%>
        <li>
          <a href='/main/<%= results[i].id %>'>
            <%= results[i].title %>
            <%= results[i].name %>
          </a>
        </li>
      <% }; %>
    </ol>
    <article>
      <% if(iresults){ %>
        <h2><%= iresults.title %></h2>
        <p> <%= iresults.content %> </p>
        <p><%= "작성자 : " + iresults.name %></p>
      <% } else { %>
        <h2>Welcome</h2>
         This is First 현웅 Board
      <% } %>
    </article>
    <ul>
        <li><a href="/write">글쓰기</a></li>
      <% if(iresults){ %>
        <li><a href='/main/<%= iresults.id %>/edit'>수정</a></li>
        <li><a href="/main/<%= iresults.id %>/delete">삭제</a></li>
      <% } %>
    </ul>
  </body>
</html>
